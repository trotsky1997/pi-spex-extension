---
description: Reconcile drift between the current spec artifacts and the implementation.
---
Help reconcile drift between the current spec artifacts and implementation.

Expectations:
- Identify mismatches between `spec.md`, `plan.md`, `tasks.md`, and the current code
- Classify each mismatch as one of:
  - spec should change
  - code should change
  - ambiguity needs clarification
- For each mismatch, explain the recommendation and trade-offs
- Prefer minimal, explicit reconciliation over vague compromise
- End with a recommended next action list

Extra context:

```text
$ARGUMENTS
```
